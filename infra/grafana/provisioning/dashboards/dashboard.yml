apiVersion: 1

providers:
  - name: FishShop
    orgId: 1
    folder: FishShop
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
